<% ignore=['uptime_seconds','--- !ruby/sym _timestamp','servername','memoryfreeinbytes','memoryfree','uptime_hours','serverip','swapfree','swapfreeinbytes','uptime'] %>
<%= scope.to_hash.reject { |k,v| !( k.is_a?(String) && v.is_a?(String) && ! ignore.include?(k) ) }.to_yaml.sort %>
